{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Round list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Degree</th>
                <th>Datecreation</th>
                <th>Datematch</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_round_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.degree }}</td>
                <td>{% if entity.dateCreation %}{{ entity.dateCreation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.dateMatch %}{{ entity.dateMatch|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_round_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_round_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_round_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
