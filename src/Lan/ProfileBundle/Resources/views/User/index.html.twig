{% extends '::base.html.twig' %}

{% block body -%}
    <h1>User list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Picture</th>
                <th>Victories</th>
                <th>Defeats</th>
                <th>Draws</th>
                <th>Teamvictories</th>
                <th>Teamdefeats</th>
                <th>Teamdraws</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_user_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.picture }}</td>
                <td>{{ entity.victories }}</td>
                <td>{{ entity.defeats }}</td>
                <td>{{ entity.draws }}</td>
                <td>{{ entity.teamVictories }}</td>
                <td>{{ entity.teamDefeats }}</td>
                <td>{{ entity.teamDraws }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_user_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_user_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_user_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
